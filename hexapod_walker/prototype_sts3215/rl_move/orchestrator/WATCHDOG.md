# Local RL fleet watchdog

This is the instruction text for the existing Codex heartbeat `keep-hexapod-rl-gpus-training`, which checks the fleet every five minutes. The app owns scheduling; this file records its intended behavior and does not create a second scheduler. Authentication stays in local configuration.

Keep the existing CoreWeave hexapod RL fleet doing useful training for Lukas. Check this thread's ongoing work every visit; this watchdog complements the cloud orchestrator and the separate physical Robot Lab watchdog.

Read live orchestrator activity, canonical capacity, queued work, actual trainer processes and recent step/FPS progression. Distinguish optimizing, compiling, evaluating and truly idle devices; RUNNING labels and allocated GPU memory do not prove training. Check NVIDIA utilization when progress disagrees. Prefer RL MCP and existing ops helpers. Public index: https://hexapod.cwd1f0-new-cluster.coreweave.app/llms.txt . Authenticated MCP is /mcp on that host; resolve the existing rl_orchestrator configuration privately without printing credentials. If MCP tools are unavailable, use authenticated JSON-RPC or approved kubectl. Controller: hexapod-sweep-friction; kubeconfig: /Users/lukas/.kube/coreweave.yaml; controller project: /workspace/hexapod/hexapod_walker/prototype_sts3215. Local project: /Users/lukas/Documents/ChatGPT/hexapod project. Use uv for Python.

Use capacity.py --json, launch_run.py and ops.sh in the controller project. Drain runnable backlog into ready slots and verify resulting processes/progress. Repair routine watcher, launcher, stale-lock and failed-run problems through existing recovery paths, preserving checkpoints and healthy workloads. If the backlog is empty while walking/steering goals remain unmet, have the orchestrator prepare and queue justified continuations or the next bounded experiments and verify placement. Keep useful work ahead of completions where dependencies allow. Do not stop at a submitted feedback note when an idle-capacity issue still needs execution.

Respect known cycle ownership, duplicate protection, leases, training budgets and spending limits. Do not repeatedly kick an advancing cycle; track the existing request and intervene only for an unowned issue or a completed/stalled request that did not address it. Preserve prior-free scratch learning and assisted walking/steering. The robotwalk-smooth-20260906 stride and turns arms began with 8M steps each, one seed each. This was an initial experiment allocation, not a permanent user-imposed cap. Scientifically justified, explicitly recorded bounded continuations are authorized by Lukas's keep-training instruction. Do not duplicate active runs or add an unplanned seed, and do not relax success criteria. On 2026-09-06 the main walking task clarified this after this watchdog mistakenly stopped cw-robotwalk-turns-20260906-cont8m; preserve that KILLED history and its retained checkpoint, and recover only the remaining portion of the same planned +8M under a documented recovery lineage. Do not stop that lineage solely because of the initial 8M allocation. Continue other authorized, hypothesis-driven work under standing campaign limits. Research feedback fb_20260906T030508_f30c55 and commit7f0b0ef87 should help combined yaw without extra report/qualification waits.

Watch for the two diagnosed utilization gaps: partial-idle refill suppressed by any running job, and completed trainers retaining GPUs during CPU video/evaluation work. Follow current repair state before repeating fixes. Finish necessary deployment/verification of in-progress watcher repairs. Keep evaluations and checkpoint provenance while making training capacity reusable.

Use allocated infrastructure. Do not buy capacity, uncordon unavailable nodes, evict healthy work, weaken physical limits or fabricate busywork. This heartbeat does not operate/deploy the robot or enqueue physical trials; the separate Robot Lab task owns those. Ordinary reads, continued training and routine recovery are authorized without another confirmation. Only a concrete unrecoverable external dependency or required new spending needs Lukas.

Persist compact records of idle periods, exact run/cycle identities, steps, queued work and repairs to avoid duplication. Report measured progress, idle capacity and recovery results accurately. Continue until Lukas asks to stop or the walking/steering objectives are achieved, then pause this heartbeat.

Deployment record from this task: watcher fix local commit 0764fd690, deployed and pushed on controller main as 3342c146. It detects partial fleet idleness and retains ready evaluation triggers until a cycle is successfully started. Eighteen offline tests passed, and thirteen new regression tests passed on the controller before adoption. The sanctioned self-restart began at 2026-09-06 03:38:29 UTC; verify the newer restart completion and cleared PAUSE/WRAPUP before considering this rollout complete, unless this thread has already recorded that verification. Do not restart again merely because an older log line says pending. Training processes are independent of this graceful decision-cycle restart.

The measured GPU retention implementation handoff is feedback fb_20260906T032210_129bed. It calls for a durable final checkpoint and a separate CPU artifact stage, preserving evaluations/videos and W&B identity. Follow up on implementation after useful work is placed; do not treat a feedback receipt as a finished fix. Current CPU scheduling constraints: train-6 requests 24 CPUs and is pinned to g142d86 with roughly 1.525 CPUs free; the other schedulable node g129004 has roughly 1.195 CPUs free. Two other nodes are cordoned. Re-read live capacity before acting; these are recorded measurements, not permanent facts. Use existing idle-only resource adjustment rules when justified, without evicting healthy work or uncordoning provider-managed nodes.

Routine MCP access was verified without a new approval prompt on 2026-09-06. Prefer native RL MCP tools. When they are absent, from the local repository root run uv run --no-project --offline python hexapod_walker/prototype_sts3215/rl_move/orchestrator/prepare_mcp_curl.py to prepare /tmp/hexapod-mcp-read.conf privately from the existing Codex configuration. This local helper performs no network I/O and prints no credentials. Then invoke curl -f -sS --max-time 30 --config /tmp/hexapod-mcp-read.conf --data with the JSON-RPC request directly, using -o for output files. The exact working request and decoding are documented in rl_move/orchestrator/README.md. Do not wrap the network call in Python, command substitution, shell redirection, or a new shell wrapper; those change the approved command shape and caused previous avoidable waits. Give delegated agents this same access method. For capacity/process operations unavailable through MCP, the existing approved CLI form is kubectl --kubeconfig=/Users/lukas/.kube/coreweave.yaml exec directly; capture returned output through the tool/client. Do not ask for operator permission for already authorized monitoring and recovery. Respect actual enforced platform permissions without disabling security settings. The watcher restart completed successfully at 2026-09-06 03:46:33 UTC, with PAUSE and WRAPUP cleared and deployed code verified; do not repeat that restart.
