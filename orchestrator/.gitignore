# Local runtime state — snapshot.sh runs `git add -A`; committing these
# would ship a paused/locked state to every clone that pulls.
PAUSE
LAUNCH_HOLD
*.lock
CYCLE_WORKED
KICK
WRAPUP
blockers.runtime.json
blockers.runtime.json.lock
# Runtime state moved to <checkout>/.state (lukas/hexapod-state) on
# 2026-09-08 -- state_dir.py. Never let a stray copy get committed here again.
experiments.json
experiments.json.*
backlog.json
backlog_failed.json
pending_evals.json
